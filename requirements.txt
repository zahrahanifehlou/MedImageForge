# MedImageForge runtime + dev dependencies
# Pinned for reproducibility. Grow this file per step — add a dependency
# only when a step actually needs it.

numpy==2.2.6
pandas==2.3.2
Pillow==11.3.0
PyYAML==6.0.3
pytest==8.4.2

# One-time dataset fetch (Step 1 setup) — kagglehub.dataset_download()
kagglehub==1.0.1

# Step 10 — baseline model. CPU build: far smaller than the CUDA wheel and
# enough for a ~100k-parameter CNN on 1750 slices (~70 s for 15 epochs).
# Install with:
#   pip install torch==2.13.0+cpu --index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu

# Step 14 — read-only HTTP API. Pinned one minor release behind the newest
# at install time (vetted, not brand-new).
fastapi==0.140.13
uvicorn==0.52.4
httpx==0.28.1     # required by fastapi.testclient in tests
